#!/usr/bin/env python3
# author: daniel rode
# tags: file management
# dependencies: python 3.12+
# created: 21 jul 2022
# updated: 20 aug 2026


"""Trash files.

(Trash)Can. Sends a given set of files to the trash. Accounts for various
platforms and available tools.
"""


import os
import sys
import uuid
import fcntl
import shutil
import subprocess as sp
from pathlib import Path
from datetime import datetime


EXE_NAME = Path(__file__).name
HELP = f"Usage: {EXE_NAME} PATH..."
HOME = Path.home()
PWD = Path.cwd()

TRASH_DIR = HOME / '.trash'


# Functions
def print2(*a, **kw):
    print(*a, **kw, file=sys.stderr)

def same_filesystem(a, b):
    """Check if two paths are on the same filesystem."""

    if os.stat(a).st_dev == os.stat(b).st_dev:
        return True
    else:
        return False

def cp_reflink_a(src, dst):
    """Copy file as reflink and preserve metadata."""

    with open(src, "rb") as s, open(dst, "wb") as d:
        fcntl.ioctl(d.fileno(), fcntl.FICLONE, s.fileno())

    shutil.copystat(src, dst)

def cp_r(src, dst):
    """Copy file or directory."""

    if src.is_dir():
        shutil.copytree(src, dst, copy_function=cp_reflink_a)
    else:
        cp_reflink_a(src, dst)

def rm_r(fp):
    """Delete file or directory."""

    if fp.is_dir():
        fp.rmdir()
    else:
        fp.unlink()

def trash(fp, trash_dir=TRASH_DIR):
    """Trash the given file/dir."""

    dst_fp = Path(
        trash_dir,
        "__".join((
            datetime.now().strftime("%Y-%m-%d-%H%M%S"),
            str(uuid.uuid4()),
            fp.name,
        )),
        'file',
    )
    dst_fp.parent.mkdir(exist_ok=True, parents=True)

    info_fp = dst_fp.parent / 'src'
    if same_filesystem(fp, trash_dir):
        info_fp.write_text(str(fp.resolve()))
        fp.rename(dst_fp)

        return

    try:
        cp_r(fp, dst_fp)
    except OSError as e:
        shutil.rmtree(dst_fp.parent)
        if str(e) in (
            '[Errno 18] Invalid cross-device link',
            '[Errno 95] Operation not supported',
        ):
            trash(fp, trash_dir=Path('./.trash'))
        else:
            raise e
    else:
        info_fp.write_text(str(fp.resolve()))
        rm_r(fp)


# Parse command line arguments
if not sys.argv[1:]:
    print2(HELP)
    sys.exit(1)

# Process files
for fp in sys.argv[1:]:
    fp = Path(fp)

    if not fp.exists():
        print("SKIPPING (path does not exist):", fp)
        continue

    if fp.is_mount():
        sp.run(['umount', '--verbose', str(fp)], check=True)

    print("Trashing:", fp)
    trash(fp)
