#!/usr/bin/env python3
# author: daniel rode
# dependencies:
#   sshfs (local)
#   dtach (on remote host)
#   ~/.crypt/private_code_vars.toml (local)
# created: 23 feb 2025
# updated: 26 mar 2026


# Alias/shortcuts for starting SSH sessions.


import sys
import shlex
import tomllib
import getpass
import subprocess as sp
from pathlib import Path


HOME = Path.home()
PWD = Path.cwd()
SSH_ARGS = (
    # Give up trying to connect if it takes too long
    '-o', 'ConnectionAttempts=64',
    '-o', 'ConnectTimeout=5',

    # Disconnect if server disappears for too long
    '-o', 'ServerAliveCountMax=4',
    '-o', 'ServerAliveInterval=11',
)
SSH_CMD = (
    # By using dtach, the user can simply close the terminal and it will
    # leave any program in the remote SSH shell running without killing it.
    'bash', '--login', '-c', f''':
        tmpdir="$(mktemp -d)"
        # Commenting out for now because Bash would remove socket file before
        # dtach was done with it if terminal closed, making it impossible to
        # connect to the socket later, even though the original dtach instance
        # was still using it.
        # function cleanup {{ rm -fr -- "$tmpdir"; }}
        # trap cleanup EXIT
        [[ -d {shlex.quote(str(PWD))} ]] && cd {shlex.quote(str(PWD))}
        dtach -c "$tmpdir/dtach.sock" -zE fish
    '''
)


def set_term_win_title(title):
    sys.stdout.write(f"\x1b]2;{title}\x07")
    sys.stdout.flush()


# Parse command line arguments
try:
    alias = sys.argv[1]
except IndexError:
    print("error: SSH remote host alias not provided")
    sys.exit(1)

# Set values for remote host
use_sock = False
match alias:
    case 's':
        host = "HOME_SERVER"
    case 'm':
        host = "WORK1"
    case 'h':
        host = "WORK2"
        use_sock = True
        SSH_CMD = ( 'bash', '--login' )
    case _:
        print("error: SSH remote host alias not defined")
        sys.exit(1)

# Import alias to host map and set remote host values
with Path(HOME, ".crypt", "private_code_vars.toml").open('rb') as f:
    private_code_vars = tomllib.load(f)

title = private_code_vars[host]['title']
ip = private_code_vars[host]['ip']

try:
    user = private_code_vars[host]['user']
except KeyError:
    user = getpass.getuser()

if use_sock:
    sock_path = Path(HOME, '.ssh', f"{user}@{ip}.sock")
    if not sock_path.exists():
        SSH_ARGS += [ '-M' ]  # Use "master" mode
    SSH_ARGS += [
        '-S', str(sock_path),
        # -f  # Authenticate, then run in background
        # -N  # Do not execute a remote command
        '-C'  # Compress all data
    ]
    

# Set terminal window title
set_term_win_title(title)

# Connect to remote host via SSH
print("Connecting...")
cmd = (
    'env', 'TERM=xterm',
    'ssh', '-t',
    *SSH_ARGS,
    '-l', user,
    ip,
    *SSH_CMD
)
sp.run(cmd)
